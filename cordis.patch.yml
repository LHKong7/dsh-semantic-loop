# The invariant is host-owned: it validates semantic messages regardless of
# which agent preset produced them. The semantic protocol itself stays inside
# the packaged `semantic` preset so installing this bundle does not change
# Standard, Code, Minimal, or user-authored modes.
- insert:
    - id: semantic-loop-invariant
      name: 'dsh-semantic-loop/invariant'
